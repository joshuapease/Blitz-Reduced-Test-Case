<!DOCTYPE html>
<html lang="en-US">
<head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta charset="utf-8" />
    <title>Welcome to Craft CMS</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
    <meta name="referrer" content="origin-when-cross-origin" />
</head>
<body class="ltr">
    {# Disable eager loading to get cache to clear properly #}
    {% set people = craft.entries
        .section('people')
        .with(['profileImage'])
        .all()
    %}

    <ul>
        {% for person in people %}
            <li>
                <img src="{{ person.profileImage.one().url }}"><br>
                {{ person.title }}<br>
                {{ person.profileImage.one().title }}
            </li>
        {% endfor %}
    </ul>
</body>
</html>
